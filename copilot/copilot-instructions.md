# Git Commit Guidelines

When creating commits:
- Always use one-line commit messages
- Extract JIRA task ID from branch name and include in commit message
- Format: `type: TASK-ID description` (e.g., `feat: DVS-4404 add feature`)
